# Turbopack's Lightning CSS prefixes and downlevels from this list, and it is
# also the JS build target. oklch(var(--x) / a) cannot be downleveled, so the
# floor is the first release that renders oklch.
chrome >= 111
edge >= 111
firefox >= 113
safari >= 15.4
ios_saf >= 15.4
not dead
